version: '3.9'

services:
  postgres:
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: anton
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: mldata

volumes:
  postgres_data: